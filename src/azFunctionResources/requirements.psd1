@{
	# Azure Functions managed dependency — modules listed here are auto-installed.
	# SqlLabDataGenerator must be bundled separately in the function app (not available via Az managed deps).
	Az = '12.5.0'
}