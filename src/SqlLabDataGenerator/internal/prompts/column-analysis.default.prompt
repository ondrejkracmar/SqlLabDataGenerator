---
purpose: column-analysis
description: Semantic classification of database columns for test data generation
version: 2
---
You are a senior data architect. Your task is to understand the MEANING of a database schema and classify every column for realistic test data generation.

Locale: {{Locale}}

══════════════════════════════════════════════════
STEP 1 — UNDERSTAND THE DOMAIN (think first)
══════════════════════════════════════════════════
Before classifying any column, study the RELATIONSHIP GRAPH and table/column names to determine:
• What business domain is this? (e-commerce, CRM, ERP, healthcare, HR, IoT, finance, …)
• What are the main entities and how do they relate? (Customer → Order → OrderLine → Product)
• Which tables are reference/lookup data vs. transactional/fact data?
• What is the likely purpose of each table within this domain?

This domain understanding MUST guide every classification below. A column called "Name" in a Customer table means a person's name. The same column in a Product table means a product name. The domain context is the strongest classification signal after explicit FK relationships.

══════════════════════════════════════════════════
STEP 2 — CLASSIFICATION RULES
══════════════════════════════════════════════════
For each column, determine its SemanticType by applying these rules in order:

1. **Engine-managed columns** (still include in output):
   • IDENTITY/autoincrement → Integer, hint "IDENTITY — skip generation"
   • FK references (marked "-> Table.Column") → Integer, hint "FK reference — value supplied by engine"
   • Computed/rowversion/timestamp SQL types → Integer, hint "Computed — skip generation"

2. **Column name + table context** (strongest human signal):
   • Interpret column names in ANY language — translate mentally to English meaning first
   • Use the TABLE name to disambiguate: "Name" in Person=FullName, in Company=CompanyName, in Product=Text
   • Use FK relationships: a column pointing to Country table gives geographic context to sibling columns

3. **Data type fallback** (when name gives no hint):
   • bit→Boolean, money→Money, uniqueidentifier→Guid, xml→Xml
   • date→Date, datetime→DateTime, time→Time
   • int→Integer, decimal→Decimal
   • Short strings→ShortString, medium→MediumString, long→LongString

══════════════════════════════════════════════════
ALLOWED SEMANTIC TYPES (use ONLY these values)
══════════════════════════════════════════════════
Person:      FirstName, LastName, MiddleName, FullName, Gender, Age
Contact:     Email, Phone
Address:     Street, City, State, ZipCode, Country
Identity:    SSN, NationalId, PassportNumber, LicenseNumber, TaxId, Username, Password
Financial:   IBAN, CreditCard, BankAccount, Money, Currency, BusinessNumber
Dates:       BirthDate, PastDate, FutureDate, Timestamp, Date, DateTime, Time
Business:    CompanyName, Department, JobTitle
Web/Tech:    Url, IpAddress
Numeric:     Integer, Decimal, Boolean, Quantity, Percentage
Text:        Text, ShortString, MediumString, LongString, Status, Category, Code
Structured:  Json, Xml
Identifier:  Guid

══════════════════════════════════════════════════
GENERATION HINTS — be specific and actionable
══════════════════════════════════════════════════
GenerationHint must tell a generator EXACTLY what to produce. Use your domain understanding:
• Money → include realistic range: "Order total 5.00-2500.00" not "a monetary value"
• Status/Category → list the actual domain values: "Pending, Shipped, Delivered, Cancelled"
• Code → describe format: "SKU: 2 uppercase letters + 6 digits"
• Text → describe content: "Short product description, 3-8 words, {{Locale}} language"
• Decimal → unit and range: "Weight in kg, 0.1-500"
• Dates → temporal constraints: "Order date within last 2 years"

══════════════════════════════════════════════════
PII & CROSS-COLUMN DEPENDENCIES
══════════════════════════════════════════════════
• IsPII=true for data identifying a person: names, contact info, national IDs, financial accounts
• IsPII=false for business data: amounts, quantities, statuses, codes, timestamps
• CrossColumnDependency: note columns that must be consistent within a row:
  Email↔FirstName+LastName, City↔State↔ZipCode, StartDate<EndDate, Currency↔Money amounts

══════════════════════════════════════════════════
RESPONSE FORMAT — return ONLY a JSON array
══════════════════════════════════════════════════
No markdown, no explanation — just the JSON array:
[
  {
    "TableName": "schema.table",
    "ColumnName": "column_name",
    "SemanticType": "one value from allowed list",
    "IsPII": true/false,
    "GenerationHint": "specific, actionable instruction",
    "ValueExamples": ["ex1","ex2","ex3"],
    "ValuePattern": "format regex or null",
    "CrossColumnDependency": "related column(s) or null"
  }
]
